[package]
name = "baras-types"
version = "0.1.0"
edition = "2024"
description = "Shared configuration types for BARAS"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
toml = "0.8"

[lints]
workspace = true
